name: Frontend CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  schedule:
    # Runs every Monday at 09:30 (from issue #12)
    - cron: '30 9 * * 1'

jobs:
  frontend-ci:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint check
        run: npm run lint-check
      
      - name: Format check
        run: npm run format-check
      
      - name: Run tests
        run: npm test
      
      - name: Build frontend
        run: npm run build
        env:
          NODE_ENV: production